$COMMON=hmac_prov.c

IF[{- !$disabled{cmac} -}]
  $COMMON=$COMMON cmac_prov.c
ENDIF

IF[{- !$disabled{gmac} -}]
  $COMMON=$COMMON gmac_prov.c
ENDIF

IF[{- !$disabled{kmac} -}]
  $COMMON=$COMMON kmac_prov.c
ENDIF

LIBS=../../../libcrypto
SOURCE[../../../libcrypto]=$COMMON
INCLUDE[../../../libcrypto]=. ../../../crypto

IF[{- !$disabled{fips} -}]
  MODULES=../../fips
  SOURCE[../../fips]=$COMMON
  INCLUDE[../../fips]=. ../../../crypto
ENDIF
