/*
 * Copyright 2025 The OpenSSL Project Authors. All Rights Reserved.
 *
 * Licensed under the Apache License 2.0 (the "License").  You may not use
 * this file except in compliance with the License.  You can obtain a copy
 * in the file LICENSE in the source distribution or at
 * https://www.openssl.org/source/license.html
 */

{-
use OpenSSL::paramnames qw(produce_param_decoder);
-}

/*
 * ml_dsa_composite_import_params: only the list and struct are needed.
 * Params are parsed manually in ml_dsa_composite_import_internal() via
 * OSSL_PARAM_locate_const(), so the generated decoder is omitted.
 */
#ifndef ml_dsa_composite_import_params_list
static const OSSL_PARAM ml_dsa_composite_import_params_list[] = {
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_PROPERTIES, NULL, 0),
    OSSL_PARAM_END
};
#endif

/*
 * ml_dsa_composite_export_params: only the list is needed.
 * Params are assembled manually in ml_dsa_composite_export(), so the generated
 * decoder is omitted.
 */
#ifndef ml_dsa_composite_export_params_list
static const OSSL_PARAM ml_dsa_composite_export_params_list[] = {
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_END
};
#endif

{- produce_param_decoder('ml_dsa_composite_get_params',
                         (['OSSL_PKEY_PARAM_BITS',          'bits',    'int'],
                          ['OSSL_PKEY_PARAM_SECURITY_BITS', 'secbits', 'int'],
                          ['OSSL_PKEY_PARAM_MAX_SIZE',      'maxsize', 'int'],
                          ['OSSL_PKEY_PARAM_PUB_KEY',       'pubkey',  'octet_string'],
                          ['OSSL_PKEY_PARAM_PRIV_KEY',      'privkey', 'octet_string'],
                         )); -}

{- produce_param_decoder('ml_dsa_composite_gen_set_params',
                         (['OSSL_PKEY_PARAM_PROPERTIES', 'propq', 'utf8_string'],
                          ['OSSL_PKEY_PARAM_PRIV_KEY',   'privkey', 'octet_string'],
                         )); -}
