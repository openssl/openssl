/*
 * Handles the loading of FIPS self test related options that are passed from
 * the core using OSSL_PARAM.
 *
 * Example usage:
 *   OSSL_FIPS_PARAM(module_filename, OSSL_PROV_PARAM_CORE_MODULE_FILENAME)
 *
 * The first parameter represents a field name in SELF_TEST_POST_PARAMS
 * to load an option into.
 * The second parameter 'OSSL_PROV_PARAM_CORE_MODULE_FILENAME' is used to
 * optionally retrieve an OSSL_PARAM option from the core.
 * Note: The module filename is a special parameter, all other parameters
 * are generated by fipsinstall.c.
 * The OpenSSL code decides what happens if the option is not present.
 */
OSSL_FIPS_PARAM(module_filename, OSSL_PROV_PARAM_CORE_MODULE_FILENAME)
OSSL_FIPS_PARAM(module_checksum_data, OSSL_PROV_FIPS_PARAM_MODULE_MAC)
OSSL_FIPS_PARAM(conditional_error_check, OSSL_PROV_FIPS_PARAM_CONDITIONAL_ERRORS)
OSSL_FIPS_PARAM(defer_tests, OSSL_PROV_FIPS_PARAM_DEFER_TESTS)
