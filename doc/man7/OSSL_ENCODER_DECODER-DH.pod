=pod

=head1 NAME

OSSL_ENCODER_DECODER-DH, OSSL_ENCODER-DH, OSSL_DECODER-DH,
OSSL_ENCODER-DHX, OSSL_DECODER-DHX
- DH and DHX encoder and decoder

=head1 DESCRIPTION

The DH and DHX encoder and decoder implementations are available in the
default and base providers.  DH uses PKCS#3 parameters while DHX uses
X9.42 parameters.

=head2 Encoding

The following output types are supported:

=over 4

=item text

Human-readable text representation of the key fields.

=item DER

Binary DER encoding.

=item PEM

PEM (base64-encoded DER with header/footer) encoding.

=back

The following output structures are supported:

=over 4

=item PrivateKeyInfo (PKCS#8)

Standard PKCS#8 unencrypted private key format.

=item EncryptedPrivateKeyInfo (PKCS#8)

Standard PKCS#8 encrypted private key format.
Requires a cipher to be specified via the B<cipher> parameter.

=item SubjectPublicKeyInfo

Standard X.509 public key format.

=item type-specific, dh, pkcs3

For DH keys, encodes key parameters.
The structure names "type-specific", "dh", and "pkcs3" are equivalent aliases.

=item type-specific, dhx, X9.42

For DHX keys, encodes key parameters.
The structure names "type-specific", "dhx", and "X9.42" are equivalent aliases.

=back

=head2 Decoding

The following input types are supported:

=over 4

=item DER

Binary DER input.

=item PEM

PEM input (automatically handled via the general PEM-to-DER decoder).

=back

The following input structures are supported for DER decoding:

=over 4

=item PrivateKeyInfo

=item SubjectPublicKeyInfo

=item type-specific, dh

For DH only.  The structure names "type-specific" and "dh" are equivalent
aliases.

=item type-specific, dhx

For DHX only.  The structure names "type-specific" and "dhx" are equivalent
aliases.

=back

=head2 Encoder Parameters

=over 4

=item "cipher" (B<OSSL_ENCODER_PARAM_CIPHER>) <UTF8 string>

The name of the cipher to use when encrypting the private key in
EncryptedPrivateKeyInfo format.  If not specified, no encryption is performed.

=item "properties" (B<OSSL_ENCODER_PARAM_PROPERTIES>) <UTF8 string>

Property query string used when fetching the cipher.

=item "save-parameters" (B<OSSL_ENCODER_PARAM_SAVE_PARAMETERS>) <integer>

If set to 0, domain parameters are not saved in the encoded output.
The default is 1 (save parameters).

=back

=head1 SEE ALSO

L<EVP_PKEY-DH(7)>, L<provider-encoder(7)>, L<provider-decoder(7)>,
L<OSSL_PROVIDER-default(7)>, L<OSSL_PROVIDER-base(7)>

=head1 HISTORY

This functionality was added in OpenSSL 3.0.

=head1 COPYRIGHT

Copyright 2020-2026 The OpenSSL Project Authors. All Rights Reserved.

Licensed under the Apache License 2.0 (the "License").  You may not use
this file except in compliance with the License.  You can obtain a copy
in the file LICENSE in the source distribution or at
L<https://www.openssl.org/source/license.html>.

=cut
