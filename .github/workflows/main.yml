/* password_cb and password_cb_data are the same kinds of parameters that you would
       pass to PEM_read_PrivateKey() and friends */
    STACK_OF(STORE_INFO) *objects = STORE_load("file:foo/bar/stuff.pem", password_cb, password_cb_data);

name: CIFuzz
on: [pull_request]
jobs:
  Fuzzing:
    runs-on: ubuntu-latest
    steps:
    - name: Build Fuzzers
      uses: google/oss-fuzz/infra/cifuzz/actions/build_fuzzers@master
      with:
        oss-fuzz-project-name: 'openssl'
        dry-run: false
    - name: Run Fuzzers
      uses: google/oss-fuzz/infra/cifuzz/actions/run_fuzzers@master
      with:
        oss-fuzz-project-name: 'openssl'
        fuzz-seconds: 600
        dry-run: false
    - name: Upload Crash
      uses: actions/upload-artifact@v1
      if: failure()
      with:
        name: artifacts
        path: ./out/artifacts

STORE_open(uri)
    loop around STORE_load() until end of data or failure
    STORE_close()
