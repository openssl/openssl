SUBDIRS=bio cipher digest keyexch mac kdf pkey signature \
        encrypt encode sslecho

IF[{- !$disabled{"cms"} -}]
    SUBDIRS=cms
ENDIF

IF[{- !$disabled{"dgram"} -}]
    SUBDIRS=guide
ENDIF

IF[{- !$disabled{"des"} -}]
    SUBDIRS=smime
ENDIF
