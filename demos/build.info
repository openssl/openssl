SUBDIRS=bio cipher digest keyexch smime mac kdf pkey signature \
        encrypt encode sslecho

IF[{- !$disabled{"cms"} -}]
    SUBDIRS=cms
ENDIF

IF[{- !$disabled{"dgram"} -}]
    SUBDIRS=guide
ENDIF
