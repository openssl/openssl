{- our $c_rehash_name =
       $config{target} =~ /^(VC|vms)-/ ? "c_rehash.pl" : "c_rehash";
   "" -}
IF[{- !$disabled{apps} -}]
  SCRIPTS={- $c_rehash_name -}
  SOURCE[{- $c_rehash_name -}]=c_rehash.in

  SCRIPTS={- handshakes_per_second -}
  SOURCE[{- handshakes_per_second -}]=handshakes_per_second.pl

  PROGRAMS=handshakes_per_second_helper
  SOURCE[handshakes_per_second_helper]=handshakes_per_second_helper.c
  INCLUDE[handshakes_per_second_helper]=.. ../include ../../include
  DEPEND[handshakes_per_second_helper]=../libcrypto.a ../libssl.a
ENDIF

